import html from "../index.html"; // Импортируем переменную html для автоматического отслеживания изменений в файле html
import css from "../style/index.css";


function summ (a,b) {
    let c = a+b;
    console.log(c);
}

summ(10,5);